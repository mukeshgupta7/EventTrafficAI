services:
  - type: web
    name: gridlock-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: "gunicorn -w 2 -k uvicorn.workers.UvicornWorker api:app --bind 0.0.0.0:$PORT"
